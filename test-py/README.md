# Pet Store — MCP Server (Python)

> Generated by [mcp-gen](https://github.com/your-org/openapi-to-mcp)
> OpenAPI version: 1.0.0

A sample Pet Store API to test mcp-gen

## Setup

```bash
pip install -r requirements.txt
```

## Run

```bash
python server.py
```

## Connect to Claude Desktop

Add to your `claude_desktop_config.json`:

```json
{
  "mcpServers": {
    "pet-store": {
      "command": "python",
      "args": ["/absolute/path/to/server.py"]
    }
  }
}
```

## Available Tools (4)

### `get_pets`

> List all pets

**`GET /pets`**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `limit` | `number` | — | Maximum number of pets to return |

### `post_pets`

> Create a pet

**`POST /pets`**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `body` | `object` | ✓ | Request body |

### `get_pets_petid`

> Get a pet by ID

**`GET /pets/{petId}`**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `petId` | `number` | ✓ | The ID of the pet to retrieve |

### `delete_pets_petid`

> Delete a pet

**`DELETE /pets/{petId}`**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `petId` | `number` | ✓ | The ID of the pet to delete |


## Implement Handlers

Find each function in `server.py` and replace the stub:

```python
@mcp.tool()
async def your_tool_name(param: str) -> Any:
    """Your tool description"""
    # @@mcp-gen:start:your_tool_name
    result = await your_api_call(param)
    return result
    # @@mcp-gen:end:your_tool_name
```

## Base URL

`https://petstore.example.com/v1`

---

*Generated by mcp-gen — edit handlers, not the scaffold.*
